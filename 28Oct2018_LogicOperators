    /**
     * APCS 28 Oct, 2017 #1 Logic Operators
     * by River.
     */
     #include <iostream>
    
    using namespace std;
    
    int main()
    {
        int a,b,ans,flag=0;
        
        cin>>a>>b>>ans;
        
        if((int)(a&&b)==ans) {
            cout<<"AND\n";
            flag=1;
        }
        if((int)(a||b)==ans) {
            cout<<"OR\n";
            flag=1;
        }
        if((int)((a==0 && b!=0) || (a!=0 && b==0))==ans) {
            cout<<"XOR\n";
            flag=1;
        }
        if(!flag) cout<<"IMPOSSIBLE\n";
    
        return 0;
    }
